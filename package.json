{
  "name": "signals",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run -s build && npm run -s jasmine",
    "test:debug": "npm run -s build && npm run -s jasmine:debug",
    "jasmine": "jasmine --config=jasmine.json",
    "jasmine:debug": "node --inspect-brk node_modules/.bin/jasmine --config=jasmine.json",
    "clean": "rm -rf dist/"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jasmine": "^4.3.0",
    "jasmine": "^4.5.0",
    "typescript": "^4.8.4"
  }
}
